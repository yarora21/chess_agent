# The store and anything derived from it — rebuildable from the API.
data/*.sqlite
data/*.sqlite-wal
data/*.sqlite-shm

# Engine binaries and weights are large, platform-specific, and not source.
weights/
bin/

# Never commit the API token.
.env

# Agent working notes — kept local. Design docs live in docs/ and are tracked.
CLAUDE.md
CLAUDE_REVIEW.md

__pycache__/
*.egg-info/
.pytest_cache/
